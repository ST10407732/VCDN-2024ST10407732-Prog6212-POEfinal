@* @model ProgPoePart2_6212.Models.SuppDocument

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Document</title>
    <style>
        body {
            background-color: #f5f5f5; /* Light grey background */
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            background-color: #d3b89f; /* Light brownish background */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            max-width: 600px;
            margin: 2rem auto;
        }

        h2 {
            margin-bottom: 1.5rem;
            color: #333;
            text-align: center;
        }

        label {
            font-weight: bold;
            display: block;
            margin-bottom: 0.5rem;
            color: #333;
        }

        select, input[type="file"] {
            width: 100%;
            padding: .375rem;
            margin-bottom: 1rem;
            border-radius: .25rem;
            border: 1px solid #ced4da;
            font-size: 1rem;
        }

        button {
            background-color: #8c5e3c; /* Brownish button */
            border: none;
            color: white;
            padding: .5rem 1rem;
            font-size: 1rem;
            border-radius: .25rem;
            cursor: pointer;
        }

            button:hover {
                background-color: #704c2f; /* Darker brown on hover */
            }

        .alert-success {
            color: white;
            background-color: #28a745;
            padding: .75rem;
            border-radius: .25rem;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Upload Document</h2>

        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success">
                @TempData["SuccessMessage"]
            </div>
        }

        <form asp-controller="LecturerClaims" asp-action="UploadDocument" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="claimId">Select Claim:</label>
                <select id="claimId" name="claimId" class="form-control" required>
                    @foreach (var claim in ViewBag.Claims)
                    {
                        <option value="@claim.Value">@claim.Text</option>
                    }
                </select>
            </div>

            <div class="form-group">
                <label for="document">Document:</label>
                <input type="file" id="document" name="document" class="form-control" required />
            </div>

            <button type="submit">Upload</button>
        </form>
    </div>
</body>
</html>
 *@